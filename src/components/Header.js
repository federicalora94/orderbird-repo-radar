import { StickyNavbar } from './StickyNavbar';  // Adjust the path as needed

function Header() {
    return (
        <header>
            <StickyNavbar />
        </header>
    );
}

export default Header;
